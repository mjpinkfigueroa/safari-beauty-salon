# This empty file tells Python that the 'models' folder is a package.
# It allows other files to import from this folder using:
#   from models.database import init_db, get_db_connection
#
# Without this file, Python wouldn't recognize 'models' as importable.
